<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ly.generalassemb.drewmahrt.bookshelf.MainActivity">

    <Button
        android:id="@+id/sort_by_title_button"
        android:text="Sort by Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/sort_by_author_button"
        android:text="Sort by Author"
        android:layout_toRightOf="@id/sort_by_title_button"
        android:layout_toEndOf="@id/sort_by_title_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <ListView
        android:id="@+id/book_list_view"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:layout_below="@id/sort_by_title_button"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</RelativeLayout>
